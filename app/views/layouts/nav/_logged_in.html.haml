%ul.nav
  = active_link_to 'Home', root_path, :wrap_tag => :li, :active => :exclusive
  = active_link_to 'My Account', account_path, :wrap_tag => :li
  = active_link_to 'My Submissions', current_user_submissions_path, :wrap_tag => :li

%ul.nav.pull-right
  = active_link_to 'Admin', admin_root_path, :wrap_tag => :li if @current_user.admin?
  = active_link_to 'Logout', logout_path, :wrap_tag => :li, :class => 'logout'
