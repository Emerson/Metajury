<%= form_for @comment, :url => submission_comments_path(@submission) do |f| %>

  <%= f.hidden_field :parent_id %>

  <%= f.text_area :content %>

  <%= f.submit 'Add Comment', :class => 'btn' %> 

  <span>or <%= link_to 'Cancel', '#', :class => 'btn-cancel-reply' %></span>

<% end %>